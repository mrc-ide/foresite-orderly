# Commits to skip in `git blame` (bulk, non-semantic reformatting).
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub applies this automatically.

# Trim trailing whitespace and normalise final newlines across all .R files
d1921cfea47d05051cfa8718f9762928f1ac125d
